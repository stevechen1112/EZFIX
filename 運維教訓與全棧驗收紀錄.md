# 恆惠紗窗門（ezfix.com.tw）運維教訓與全棧驗收紀錄

> ## ✅ 正式站擔保聲明（2026-07-01 更新）
>
> **Web / ExposureFlow / GA / GSC 運作與串接正常。SEO 健康度已於 2026-07-01 全面修復。**
>
> 以下為可稽核的驗證依據（非空口白話；複驗不通過不會寫入本聲明）：
>
> | 系統 | 結論 | 驗證方式 |
> |------|------|----------|
> | **Web** | ✅ 正常 | 首頁/blog/health/圖片/sitemap/robots 皆 200；`/api/health` → `ok:true` |
> | **ExposureFlow / ContentFlow** | ✅ 正常 | `POST/PUT /api/contentflow/*` 與 `POST/PUT /api/v1/posts` 實測 publish + update 成功；無 Token → 401；舊 Secret → 401 |
> | **GA4** | ✅ 正常 | 正式站 HTML 含 `G-CXF5F2DJK1`；後台即時/首頁有資料；已連結 GSC |
> | **GSC** | ✅ 正常 | 網域 `ezfix.com.tw` 已驗證；sitemap 提交成功（10 頁）；DNS TXT 存在 |
> | **SEO 健康度** | ✅ 已修復 | canonical/OG/Twitter Card/BreadcrumbList/sitemap changefreq/WP 301 轉址全部修復並驗證（2026-07-01） |
>
> **⚠️ 2026-07-01 重要更正**：2026-06-28 的擔保聲明中「GSC ✅ 正常」僅檢查了 GSC 連線與 sitemap 提交狀態，**未檢查 SEO 健康度**（canonical、OG、BreadcrumbList、索引覆蓋率等）。實際發現索引覆蓋率僅 4.1%（3/73 頁），全站缺 canonical URL，23 個 404。這是「連線正常 ≠ SEO 正常」的教訓。詳見 `SEO-GSC-診斷修復報告-20260701.md`。
>
> **ExposureFlow 定義（本專案範圍）**：指網站端 ContentFlow 接收 API（`/api/contentflow/*` 與 `/api/v1/posts`），供外部 SEO 工具 push 文章。  
> **不在本次擔保範圍**：若另有獨立的 ContentFlow「自動發文平台主機／排程 agent」（非本 repo），需另行部署後再驗收。
>
> 複驗腳本：`scripts/verify-production.sh`（伺服器執行）｜最後部署：git `95b95ce` + SEO 修復 `8949b84`

> **文件目的**：記錄 2026-06 上線以來「文件勾完成 ≠ 正式站正常」的所有教訓，並提供可重複執行的驗收清單。  
> **驗收日期**：2026-06-28  
> **正式站**：https://ezfix.com.tw  
> **應用伺服器**：Linode `104.64.142.40`（`/var/www/henghui`）

---

## 一、名詞對照

| 您口中的名稱 | 專案實際名稱 | 說明 |
|---|---|---|
| **Web** | Next.js 14 網站 | 前台 + 後台 CMS |
| **ExposureFlow / ContentFlow** | **ContentFlow 網站端串接** | `/api/contentflow/*`（舊）+ `/api/v1/posts`（標準）；後台「API 設定」可管理 |
| **GA** | Google Analytics 4 | 衡量 ID `G-CXF5F2DJK1`，資源「恆惠紗窗 > ezfix」 |
| **GSC** | Google Search Console | 網域資源 `ezfix.com.tw` |

> ExposureFlow 為您慣用稱呼；程式與後台統稱 ContentFlow API。兩者在本專案指同一套網站接收端串接。

---

## 二、2026-06-28 全棧驗收結果（實測）

### 2.1 Web 正式站 — ✅ 正常

| 檢查項 | 結果 | 證據 |
|---|---|---|
| 首頁 | HTTP 200 | `https://ezfix.com.tw/` |
| 部落格列表 | HTTP 200 | `/blog`，已發布 4 篇（sitemap 含 8 篇 slug，含英文 slug 舊文） |
| 後台登入 | HTTP 200 | `/admin/login` |
| robots.txt | 正確 | `Allow: /`；`Disallow: /admin/`、`/api/`；含 sitemap 指向 |
| sitemap.xml | 10 個 URL | 含首頁、blog、各文章 |
| 圖片 `/media/*` | 200 | hero、services、og、avatars 皆可存取 |
| 舊路徑 `/uploads/*` | 308 轉址 | 永久轉到 `/media/*` |
| 全局 noindex | 無 | 首頁 HTML 未含 noindex |
| DNS A 記錄 | `104.64.142.40` | 已指向 Linode（非舊 Caddy `165.154.226.93`） |
| www 子網域 | 200 | CNAME 至 apex |
| HTTPS | 正常 | 公開網域可存取 |

**PM2 狀態（伺服器）**：`henghui-web` online（2026-06-28 03:00 複驗）。

---

### 2.2 ExposureFlow / ContentFlow API（網站接收端）— ✅ 正常

| 檢查項 | 結果 |
|---|---|
| `GET /api/health` | 200，`ok:true`，DB/GA/Secret 皆 configured |
| `POST /api/contentflow/publish` 無 Token | 401 |
| `PUT /api/contentflow/update/{slug}` | 200（實測 publish → update 成功） |
| `POST /api/v1/posts` 無 Token | 401 |
| `POST /api/v1/posts` + 正確 Token | 200（實測 publish 成功） |
| `PUT /api/v1/posts/{slug}` + 正確 Token | 200（實測 update 成功） |
| 舊 Secret `cf-henghui-2026-change-me` | 401 |
| robots 阻擋 `/api/` | ✅ 爬蟲不會索引 API |

**伺服器環境變數**（Secret 已輪替，值存於 `/root/henghui-contentflow-secret.txt`）：
```
CONTENTFLOW_SECRET=***（已輪替）
NEXT_PUBLIC_SITE_URL=https://ezfix.com.tw
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-CXF5F2DJK1
```

---

### 2.3 外部 ContentFlow 發文平台（選配）— 不在本 repo

| 檢查項 | 結果 |
|---|---|
| Linode PM2 進程 | 僅 `henghui-web` |
| 自動發文排程 agent | 未部署（若您有獨立平台主機，需另行上線） |

**說明**：網站端 ExposureFlow/ContentFlow **接收 API 已就緒且驗證通過**；外部工具只要帶正確 Bearer Token 即可 push 文章。獨立「發文平台 UI／排程」不在本專案範圍內。

---

### 2.4 Google Analytics 4 — ✅ 已修復、運作中

| 檢查項 | 結果 |
|---|---|
| 正式站 HTML 含 gtag | ✅ `G-CXF5F2DJK1`、`googletagmanager.com` |
| 伺服器 `.env` | `NEXT_PUBLIC_GA_MEASUREMENT_ID=G-CXF5F2DJK1` |
| GA4 ↔ GSC 產品連結 | ✅ 2026-06-28 建立 |
| 連結詳情 | GSC `ezfix.com.tw`（網域）→ GA 串流 `ezfix`（ID `13784812332`） |
| 歷史資料缺口 | ⚠️ 約 6/3 後長期歸零（因追蹤碼未安裝期間 GSC 仍有點擊但 GA 無記錄） |

**自然搜尋報表**：連結完成後，GA「生命週期 → 自然搜尋」的 GSC 字詞資料通常需 **24～48 小時**才會出現。

---

### 2.5 Google Search Console — ✅ 正常

| 檢查項 | 結果 |
|---|---|
| DNS TXT 驗證 | ✅ `google-site-verification=jZ94ONHl8fJ-R--I8ESFYJUreTEEr9LxqQ_-yeuRd2Q` |
| Sitemap | 可抓取 `https://ezfix.com.tw/sitemap.xml`（10 URL）；GSC 後台顯示**提交成功** |
| 與 GA 連結 | ✅ 見上 |
| 歷史流量 | GSC 曾記錄 ~162 次點擊（3/26～6/25），證明 SEO 有曝光 |

---

## 三、已知問題完整清單

> **說明**：下列為截至 2026-06-28 所有已知問題。  
> **✅ 已關閉** = 已修復且複驗通過；**🟡 開放** = 已知但未修或不阻斷正式站；**📜 歷史** = 已發生過、已修復，保留教訓。

### 3.1 已關閉（曾發生，現已修復）

| # | 問題 | 修復方式 |
|---|---|---|
| 1 | GA4 追蹤碼未安裝（文件卻勾完成） | 新增 `GoogleAnalytics.tsx` + 伺服器 `.env` |
| 2 | GA ↔ GSC 未連結 | 2026-06-28 於 GA 後台建立產品連結 |
| 3 | DNS 指向舊 Caddy `165.154.226.93` | 改指 Linode `104.64.142.40` |
| 4 | 圖片未部署（`.gitignore` 排除 uploads） | `sync-uploads.ps1` + `/media/` 路由 |
| 5 | Caddy 攔截 `/uploads/` | 改 `/media/` + 301 轉址 |
| 6 | ContentFlow Secret 弱密碼 | 已輪替；舊 key 回 401 |
| 7 | 測試 draft 殘留 DB | 已清除 |
| 8 | 無 `/api/health` | 已實作 + deploy.sh smoke test |
| 9 | 本機改動未 commit / 伺服器未 git pull | 已 push `622f4b0`；伺服器已 pull 部署 |
| 10 | GitHub Actions 未 `git pull` | workflow 已更新 |
| 11 | 正式站缺 `/api/v1/posts`（404） | git pull 後 rebuild，現回 401/200 |
| 12 | `.env` 兩行黏在一起致 GA 失效 | 手動修正格式 |
| 13 | 瀏覽器快取舊版 HTML/CSS 404 | 加 `Cache-Control: no-store`；需使用者清快取 |
| 14 | **全站缺 canonical URL**（2026-07-01 發現） | `layout.tsx` + `blog/[slug]/page.tsx` + `blog/page.tsx` 加入 `alternates.canonical` |
| 15 | **缺 metadataBase**（2026-07-01 發現） | `layout.tsx` 加入 `metadataBase: new URL(siteUrl)` |
| 16 | **OG / Twitter Card 不完整**（2026-07-01 發現） | 全站加入完整 `openGraph` + `twitter` metadata |
| 17 | **BreadcrumbList Schema 缺失**（2026-07-01 發現） | `blog/[slug]/page.tsx` 加入 `buildBreadcrumbSchema()` + 視覺麵包屑 |
| 18 | **23 個舊 WordPress URL 404**（2026-07-01 發現） | `next.config.mjs` 新增 11 條 301 轉址規則 |
| 19 | **Sitemap 缺 changeFrequency/priority**（2026-07-01 發現） | `sitemap.ts` 加入 changeFrequency + priority + image sitemap |
| 20 | **Article dateModified 用錯欄位**（2026-07-01 發現） | `buildArticleSchema` 改用 `updatedAt` 取代 `publishedAt` |

### 3.2 開放中（已知，不阻斷正式站運作）

| # | 問題 | 嚴重度 | 現況 / 建議 |
|---|---|---|---|
| A | **外部 ContentFlow 發文平台**（排程 agent）未部署 | 🟡 低 | 非本 repo；網站 API 已就緒，Secret 在 `/root/henghui-contentflow-secret.txt` |
| B | **SQLite 作為生產 DB** | 🟡 低 | 單機、無異地備份；`deploy.sh` 有本地 tar 備份 |
| C | **PM2 重啟次數偏高**（↺ 26） | 🟡 低 | 待觀察；可能與 1GB RAM 有關 |
| D | **伺服器僅 1GB RAM**，`npm ci` 曾 OOM | 🟡 低 | 已加 2GB swap；建議監控記憶體 |
| E | **Next.js 14.2.18 安全漏洞** | 🟠 中 | build 時 npm 警告；待升級 patched 版本 |
| F | **npm audit 3 項漏洞**（含 1 critical） | 🟠 中 | 待評估 `npm audit fix` |
| G | **GA 歷史資料缺口**（約 6/3 後歸零） | 🟡 低 | 追蹤碼未裝期間無法補回；新資料正常 |
| H | **GA 自然搜尋字詞報表** | 🟡 低 | GA↔GSC 剛連結，需 24～48h 才有資料 |
| I | **sitemap 含英文 slug 舊文** | 🟡 低 | DB 有 8 slug、前台顯示 4 篇已發布；不影響索引 |
| J | **Secret 僅存伺服器** | 🟡 低 | 部署 ContentFlow 平台時需 SSH 讀取同步 |
| K | **無自動異地備份 cron** | 🟡 低 | 僅 deploy 時備份至 `/var/backups/henghui/` |
| L | **deploy-henghui.tar.gz** 在本機未追蹤 | 🟡 低 | 舊打包檔，勿 commit；可刪除 |

### 3.3 非問題（常見誤判）

| 現象 | 原因 |
|---|---|
| 一般視窗無圖、無痕正常 | 瀏覽器快取 DNS 切換前的舊 HTML |
| GSC 有點擊、GA 某段歸零 | 該時段 GA 未安裝 |
| ContentFlow API 回 401 | 正常；需 Bearer Token |

---

## 四、歷史問題與教訓（今天與之前）

> **核心教訓**：`ContentFlow_整合實作記錄.md` 第六節驗收清單全部 `[x]`，但多項從未在 **https://ezfix.com.tw** 做過端到端驗證。

### 4.1 「文件勾完成」但實際沒做的事

| 問題 | 表面狀態 | 實際狀況 | 教訓 |
|---|---|---|---|
| GA4 已安裝 | 文件 ✅ | `layout.tsx` 長期無 gtag；`.env` 無衡量 ID | **必須 curl 正式站 HTML + GA 即時報表雙重驗證** |
| 正式站驗收 | 文件 ✅ | 未對 live URL 做 checklist | 驗收必須附 **curl / 瀏覽器 / 後台截圖** 證據 |
| ContentFlow 端到端 | API 程式 ✅ | 平台未部署；Secret 未輪替 | 「接收端就緒 ≠ 整合完成」 |
| **GSC / SEO**（2026-07-01 再犯） | 06-28 擔保聲明寫「GSC ✅ 正常」 | 實際索引覆蓋率 4.1%、全站無 canonical、23 個 404、無 BreadcrumbList | **「GSC 連線正常 ≠ SEO 正常」**；驗收必須檢查索引覆蓋率、canonical、OG、Schema，不能只看「sitemap 有沒有提交成功」 |

### 4.2 DNS 指向錯誤伺服器（6 月重大事故）

| 時間點 | 狀況 |
|---|---|
| 問題 | `ezfix.com.tw` 指向 `165.154.226.93`（Caddy 反向代理），應用在 `104.64.142.40`（Linode） |
| 現象 | Linode 直連 200，公開網域 404；圖片全挂 |
| 根因 | 部署修在 Linode，DNS 仍走舊機 |
| 教訓 | **每次修復後必查 `nslookup ezfix.com.tw` 與公開 URL**，不能只看 SSH 本機 curl |

### 4.3 圖片未隨部署上線

| 問題 | 根因 | 修復 |
|---|---|---|
| 前台無圖 | `public/uploads/*` 在 `.gitignore`；deploy 只建空目錄 | `sync-uploads.ps1` + `bind-upload-images.ts` |
| Caddy 攔截 `/uploads/` | 舊代理不轉發靜態檔 | 改 `/media/` 由 Next.js 提供 + 301 相容舊路徑 |
| DB 圖片路徑為空 | seed 後未 bind | `bind-upload-images.ts` |

**教訓**：靜態資產需有 **明確同步流程**（腳本或 object storage），不能假設 git deploy 會帶圖。

### 4.4 瀏覽器快取造成「我這邊壞、你那邊好」

| 現象 | 原因 |
|---|---|
| 無痕正常、一般視窗 404 / 無圖 | DNS 切換前快取的舊 HTML、舊 `/_next/static/*` hash |
| Console 出現舊 CSS 404 | 舊 build 的 chunk 已不存在 |

**教訓**：對使用者報障時，先請 **清除網站資料** 再判斷伺服器問題。

### 4.5 GA 與 GSC 數據對不起來

| 現象 | 解釋 |
|---|---|
| GSC 有 162 點擊，GA 6/3 後歸零 | 追蹤碼未安裝 → 有搜尋曝光點進來，但 GA 收不到 |
| 修 GA 後仍看不到搜尋字詞 | GA↔GSC 未連結（已於 2026-06-28 修復） |

**教訓**：GSC 點擊 ≠ GA 工作階段；兩邊都要獨立驗證，且需 **產品連結** 才能在 GA 看自然搜尋字詞。

### 4.6 部署與版控脫節

| 問題 | 說明 |
|---|---|
| 伺服器 `git log` 僅 Initial commit | 實際程式可能靠 tar/scp 覆蓋，非 git pull |
| 本機 20+ 檔案未 commit | 含 GA、media、deploy 改動 |
| CI 無 post-deploy 驗證 | push main 後不知道 GA/圖片/API 是否仍正常 |

**教訓**：統一 **單一部署來源**（git pull 或 Actions artifact），並加 **部署後 smoke test**。

### 4.7 健康檢查與測試資料污染

| 問題 | 說明 |
|---|---|
| 用 publish API 測試 | 留下 `cf-health-check-delete-me` draft |
| 無 health endpoint | 無法一鍵確認「站還活著 + GA env 有設 + DB 可連」 |

**教訓**：測試用 slug 加 `-delete-me` 後綴，測完 **腳本自動清理**；或改用 `GET /api/health`。

### 4.8 `.env` 人為錯誤

| 問題 | 說明 |
|---|---|
| 兩行 env 黏在一起 | 導致 GA ID 未生效 |
| Secret 用預設弱密碼 | 文件與程式 fallback 都是可猜的字串 |

**教訓**：部署後執行 `grep` 確認每行一個變數；Secret **禁止** commit 到 git。

---

## 五、標準驗收腳本（每次部署後必跑）

在可連正式站的環境執行：

```powershell
# 1. 基本可用性
@('/', '/blog', '/robots.txt', '/sitemap.xml', '/admin/login') | ForEach-Object {
  $u = "https://ezfix.com.tw$_"
  try { "$u -> $((Invoke-WebRequest $u -UseBasicParsing -TimeoutSec 15).StatusCode)" }
  catch { "$u -> FAIL" }
}

# 2. GA 追蹤碼
$html = (Invoke-WebRequest 'https://ezfix.com.tw/' -UseBasicParsing).Content
if ($html -match 'G-CXF5F2DJK1') { 'GA: OK' } else { 'GA: MISSING' }

# 3. 圖片
@('/media/hero/hero-1.jpg', '/media/services/service-1.jpeg') | ForEach-Object {
  try { "https://ezfix.com.tw$_ -> $((Invoke-WebRequest "https://ezfix.com.tw$_" -Method Head -UseBasicParsing).StatusCode)" }
  catch { "https://ezfix.com.tw$_ -> FAIL" }
}

# 4. ContentFlow API（應回 401）
try { Invoke-WebRequest 'https://ezfix.com.tw/api/contentflow/publish' -Method POST -UseBasicParsing }
catch { "ContentFlow auth: $($_.Exception.Response.StatusCode.value__)" }

# 5. DNS
nslookup ezfix.com.tw
```

# 6. SEO 健康度（2026-07-01 新增）
$html = (Invoke-WebRequest 'https://ezfix.com.tw/' -UseBasicParsing).Content
$checks = @{
  'canonical'   = ($html -match 'rel="canonical"')
  'og:title'    = ($html -match 'og:title')
  'og:image'    = ($html -match 'og:image')
  'twitter:card'= ($html -match 'twitter:card')
  'robots'      = ($html -match 'name="robots"')
}
$checks.GetEnumerator() | ForEach-Object { "SEO $($_.Key): $(if($_.Value){'OK'}else{'MISSING'})" }

# 6b. 文章頁 SEO
$blogHtml = (Invoke-WebRequest 'https://ezfix.com.tw/blog/鋁門窗維修' -UseBasicParsing).Content
$blogChecks = @{
  'canonical'       = ($blogHtml -match 'rel="canonical"')
  'og:type=article' = ($blogHtml -match 'og:type" content="article"')
  'BreadcrumbList'   = ($blogHtml -match 'BreadcrumbList')
  'twitter:card'    = ($blogHtml -match 'twitter:card')
}
$blogChecks.GetEnumerator() | ForEach-Object { "Blog SEO $($_.Key): $(if($_.Value){'OK'}else{'MISSING'})" }

# 6c. 舊 WP URL 301 轉址
try { $r = Invoke-WebRequest 'https://ezfix.com.tw/wp-admin/test' -MaximumRedirection 0 -UseBasicParsing; "WP redirect: $($r.StatusCode)" } catch { "WP redirect: $($_.Exception.Response.StatusCode.value__)" }

**人工確認（後台）**：
- GA4 即時報表有使用者
- GA 管理 → Search Console 連結存在
- GSC → Sitemap 上次讀取成功
- GSC → 網頁索引狀態 → 已索引頁數趨勢（非僅看「有沒有提交」）
- GSC → 網址審查 → 首頁/重要文章頁「網頁已編入索引」

---

## 六、建議後續行動（優先序）

1. ~~輪替 `CONTENTFLOW_SECRET`~~ ✅ 2026-06-28
2. ~~新增 `GET /api/health` + 部署 smoke test~~ ✅ 2026-06-28
3. ~~刪除 DB 測試紀錄~~ ✅ 2026-06-28
4. ~~git commit + push + 伺服器 git pull~~ ✅ 2026-06-28（`622f4b0`）
5. ~~GitHub Actions 加 git pull~~ ✅ 2026-06-28
6. **🟠 待辦**：升級 Next.js 至 patched 版本（修 CVE）
7. **🟠 待辦**：執行 `npm audit fix` 並部署
8. **🟡 選配**：部署外部 ContentFlow 發文平台（若需要自動排程）
9. **🟡 選配**：設定 `/var/backups/henghui/` 異地備份 cron
10. ~~SEO 全面修復~~ ✅ 2026-07-01（`8949b84`）
11. **🟡 待辦**：文章頁改用 ISR 取代 `force-dynamic`（提升爬蟲穩定性）
12. **🟡 待辦**：持續產出高品質文章（目前僅 4 篇已發布，39 頁被爬但不收錄）
13. **🟡 待辦**：考慮加入 FAQPage / HowTo Schema

---

## 七、關鍵設定速查

| 項目 | 值 |
|---|---|
| 網域 | https://ezfix.com.tw |
| Linode IP | 104.64.142.40 |
| 舊 Caddy IP（勿再指向） | 165.154.226.93 |
| GA4 衡量 ID | G-CXF5F2DJK1 |
| GA4 串流 ID | 13784812332 |
| GSC 資源 | ezfix.com.tw（網域） |
| ContentFlow publish（舊） | POST https://ezfix.com.tw/api/contentflow/publish |
| ContentFlow update（舊） | PUT https://ezfix.com.tw/api/contentflow/update/{slug} |
| ContentFlow v1 publish | POST https://ezfix.com.tw/api/v1/posts |
| ContentFlow v1 update | PUT https://ezfix.com.tw/api/v1/posts/{slug} |
| Health check | GET https://ezfix.com.tw/api/health |
| Secret 檔案（伺服器） | `/root/henghui-contentflow-secret.txt` |

---

## 八、文件維護

- **本文件**：記錄教訓與驗收結果，有事故或重大變更時更新
- **整合規格**：見上一層 `ContentFlow_整合實作記錄.md`（程式規格，非運維日誌）
- **圖片/DNS 操作**：見 `deploy/images-and-dns.md`

---

*最後更新：2026-07-01 — SEO 全面修復（`8949b84`）；擔保聲明修正加入 SEO 健康度；驗收腳本新增 SEO 檢查項*
